public class Ball {
    private int matchId, inningsId, overNo, ballNo, batsmanId, bowlerId, runsScored, isWicket;

    public Ball(int matchId, int inningsId, int overNo, int ballNo, int batsmanId, int bowlerId, int runsScored, int isWicket) {
        this.matchId = matchId;
        this.inningsId = inningsId;
        this.overNo = overNo;
        this.ballNo = ballNo;
        this.batsmanId = batsmanId;
        this.bowlerId = bowlerId;
        this.runsScored = runsScored;
        this.isWicket = isWicket;
    }

    // Getters and setters (if needed)
}
